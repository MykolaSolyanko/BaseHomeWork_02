// Best divisor.cpp

#include <iostream>

int main() {

    int a, b;
    std::cin >> a >> b;
    for (int i = a; i > 0; i--) {
        if (a % i == 0 && b % i == 0) {
            std::cout << "nod = " << i;
            break;
        }
    }
    return 0;
}
